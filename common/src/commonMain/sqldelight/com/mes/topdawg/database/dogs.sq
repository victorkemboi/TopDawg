CREATE TABLE SqlDog(
id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
name TEXT NOT NULL,
profileImageUrl TEXT,
bio TEXT,
breed INTEGER
);

insertItem:
INSERT OR REPLACE INTO SqlDog VALUES(?,?,?,?,?);

selectAll:
SELECT * FROM SqlDog;

deleteAll:
DELETE FROM SqlDog;